import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.cubebaby.presets',
  appName: 'Cube Baby Presets',
  webDir: 'dist/renderer'
};

export default config;
